{% extends 'layout_admin.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block contentAdmincontent %}

    <h1>Création Categorie</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('category_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}

