{% extends 'layout_admin.html.twig' %}

{% block contentAdmincontent %}

    {{ form(form) }}
    {{ dump(commandes) }}
    {{ dump(users) }}

    {{ dump(data) }}

    {{ dump(commandesSearch) }}

{% endblock %}